EXPO_PUBLIC_API_URL=https://api.vyaha.com
# Optional. Previously required for in-app Google Maps tiles (those hung forever when
# the Android Maps SDK key/SHA did not match). In-app preview now uses OpenStreetMap.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
